/*
    variables, 
    datatypes,
    array,
    object,
    json,
    condi. state
    loop
    function    arrow
    heigher order fun
        .forEach, .map, .filter, .reduce

        .forEach((item, index, currArr)=>console.log(item))
        .map((item, index, currArr)=>console.log(item))
    
    built-in fun
    callback
    async-await


    demo(10, (res)=>{

    })

    let hello = async()=>{
        let res =await demo(10)
    }

    
    




*/